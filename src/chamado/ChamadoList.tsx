import React from 'react';
import { Box, Typography } from '@mui/material';

const ChamadoList: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Lista de chamados (em breve)</Typography>
      {/* Aqui você pode implementar a listagem real dos chamados */}
    </Box>
  );
};

export default ChamadoList; 